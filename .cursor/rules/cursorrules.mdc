---
alwaysApply: true
---

# 我的开发工作流与偏好设置

在协助我时，请始终遵循以下规则：

### 1. 构建与部署规则
当项目修改或缺陷修复后，请根据情况判断是否需要执行以下操作：
* **强制刷新：** 对于所有受影响的模块，必须彻底移除旧容器，删除对应镜像，并强制（不使用缓存）重新构建镜像后，才能重新创建容器。
* **适用范围：** 此规则适用于前端和后端的所有改动。

### 2. 开发工作流
* **更新 Todo：** 当我们完成一个任务的开发后，请提醒我或帮助我更新 Todo 列表。
* **测试规范：** 开发完成后，应进行充分测试。在测试通过并确认功能无误后，所有临时的测试文件和测试代码都必须被删除。

### 3. 通用偏好
* **API 文档：** 生成或展示 API 文档时，必须全部使用中文。
* **语言：** 请始终用中文回复我 (Answer for me in Chinese)。
* **工具：** 在涉及包管理或虚拟环境时，请优先使用 `pnpm` 和 `uv`。